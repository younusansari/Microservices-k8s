apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  type: NodePort
  selector:
    app: order
  ports:
      - port: 3002
        targetPort: 3002
        nodePort: 30002
