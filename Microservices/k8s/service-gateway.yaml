apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  labels:
    app: gateway-service
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
      - port: 3003
        targetPort: 3003
        nodePort: 30003
